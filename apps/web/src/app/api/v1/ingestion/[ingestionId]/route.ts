import { actor,safe } from '@/lib/route';import { ok } from '@/lib/http';import { db } from '@/lib/db';
export async function GET(_:Request,{params}:{params:Promise<{ingestionId:string}>}){return safe(async()=>{const a=await actor();const {ingestionId}=await params;const row=await db.ingestionRecord.findFirst({where:{ingestionId,tenantId:a.tenantId},select:{ingestionId:true,productState:true,implementationState:true,parseAttemptCount:true,lastErrorCode:true,lastErrorMessage:true,tripId:true,documentId:true,createdAt:true,updatedAt:true}});if(!row)throw new Error('NOT_FOUND: ingestion');return ok(row)})}
